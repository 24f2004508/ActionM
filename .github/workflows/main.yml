name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: 24f2004508@ds.study.iitm.ac.in
        run: echo "Hello, GitHub Actions!"
